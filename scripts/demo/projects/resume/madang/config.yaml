# 이력서 설정. 앱(core)이 읽는다. 에이전트는 이 파일을 받지 않는다.

# .madang/을 git에 넣지 않는다(이 폴더는 git 저장소가 아니다).
track: false

# 게시: docs/의 문서를 site/ 폴더에 정적 사이트로 만든다. 뷰어는 게시 시점
# 사본으로 함께 들어간다.
publish:
  include: [docs]
  target: folder:site

# 정책: 문서가 바뀌면 자동으로 게시한다.
policy:
  auto_publish: true
