# Brief

이마당의 이력서다. 문서는 `docs/resume.md`, 내용은 `docs/resume.json`에 있다.

- 화면은 이 프로젝트의 `resume/basic` 뷰어가 `docs/resume.json`을 그린다.
- 내용을 바꿀 때는 `docs/resume.json`만 고친다. 뷰어 폴더는 고치지 않는다.
- 경력은 최근 것이 위로 오게 둔다. 수치는 원래 적힌 것만 쓴다.
- `site/`는 게시가 만드는 폴더다. 손으로 고치지 않는다.
