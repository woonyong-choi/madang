# docs

이 폴더 바로 아래의 문서는 **승인된 것**만 둔다.

- `docs/ai/`: AI가 쓴 초안. 파일 머리에 "상태: 초안 (AI 작성, 미승인)"이 있다.
- 승인은 사람이 파일을 `docs/`로 옮기고 그 줄을 지우는 커밋으로 표현한다. 이력이 git에 남는다.
- `docs/ai/reference/`: 조사·목업 자료. 승격 대상이 아니다.
