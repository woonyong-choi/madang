# templates

내장 템플릿. 각 템플릿은 `template.yaml`, `page.html`, `schema.json`, `sample.json`, `theme.json`, `preview.png`.

`_runtime/madang.js`: 렌더링, data-bind, 요소↔데이터 역추적, 앱 브리지.

Phase A3에서 table, decisions, tasks, resume 네 개를 만든다.
